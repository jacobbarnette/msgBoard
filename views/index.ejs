<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <a class='msgBtn' href="/new">New Message</a>
    <h1><%= title %></h1>
  
    <div id="msgContainer">
      <% for(const msg of messages) {%>
        <div class='msgCards'>
          <ul>
              <li><strong><%=msg.user%></strong></li>
              <li>  <%=msg.text%></li>
              <li>  <%=format(msg.added)%></li>
          </ul>
        </div>
        <% } %>
        </div>
  </body>
</html>
